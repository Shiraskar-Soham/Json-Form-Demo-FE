import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './dynamicCard/App';
// import App from './accessRequestListing/App'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

